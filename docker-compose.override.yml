version: "3.9"

# Exposes individual service ports for direct debugging without going through gateway.
# Used in development only.

services:
  monolith:
    ports:
      - "5059:8080"

  # Phase 2 ✅
  identity-svc:
    ports:
      - "5001:8080"

  # Phase 3 ✅
  sentiment-svc:
    ports:
      - "5005:8080"

  # Uncomment each block as the service is extracted:

  # portfolio-svc:
  #   ports:
  #     - "5002:8080"

  # marketdata-svc:
  #   ports:
  #     - "5003:8080"
  #     - "5013:8081"   # gRPC

  # alert-svc:
  #   ports:
  #     - "5004:8080"

  # notification-svc:
  #   ports:
  #     - "5006:8080"
