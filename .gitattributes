# Normalize line endings to LF on commit, check out as-is
* text=auto

# Force LF for shell scripts (avoid CRLF breaking shebang lines)
*.sh text eol=lf

# Treat binary files as binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
